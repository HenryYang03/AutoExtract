# Uses default weights baked into the image. To override with a local file, uncomment:
# volumes:
#   - ./backend/models/best.pt:/app/backend/models/best.pt:ro
services:
  autoextract:
    build: .
    ports:
      - "9000:9000"
    environment:
      DEBUG: "false"
